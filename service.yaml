apiVersion: v1
kind: Service
metadata:
  name: hello-nestjs-service
spec:
  type: NodePort
  selector:
    app: hello-nestjs
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007